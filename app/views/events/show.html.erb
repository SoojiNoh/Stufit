<h4>정보게시판</h4><br>

<div class="progress">
  <div class="determinate" style="width: 70%"></div>
</div>

<!--제목부분-->

<h5> 제목 | <%= @event.title %> </h5>
<h6> 작성일 :  <%= @event.created_at.in_time_zone("Asia/Seoul").strftime("%Y. %m. %d.    %H : %M") %> </h6>
<h6> 조회수 : <%=@event.impressions.count%></h6>
<div class="progress">
  <div class="determinate" style="width: 70%"></div>
</div>

<!--내용부분-->

<br><p><%= @event.content %></p><br>
<%= image_tag "#{@event.image}" %><br>
<!--그 외 정보-->

장소 : <%= @event.place %><br>
분류 : <%= @event.event_type %><br>
일정 : <%= @event.start_at.in_time_zone("Asia/Seoul").strftime("%Y. %m. %d.  %H : %M") %>
     ~  <%= @event.end_at.in_time_zone("Asia/Seoul").strftime("%Y. %m. %d.  %H : %M") %><br>
     첨부파일 <br><%= @event.file %>

   <div class="progress">
     <div class="determinate" style="width: 70%"></div>
   </div>
   <h3>관련 태그</h3>
   <p>
   <% @event.hash_tags.each do |hash| %>
     <div class="chip"> <%= link_to "#{hash.name}", hash_tag_path(hash) %> </div> &nbsp;
   <% end %>
   </p>



   <!--버튼 기능-->

   <%= link_to "수정", edit_event_path(@event),class: 'waves-effect waves-light btn' %>
   <%= link_to "삭제", event_path(@event), method: "delete",class: 'waves-effect waves-light btn' %>
   <%= link_to "목록", events_path,class: 'waves-effect waves-light btn' %>
   <%= link_to '새 글쓰기', new_event_path ,class: 'waves-effect waves-light btn' %>

   <% @event.comments.each do |comment| %>
     <h4>
     <%= comment.user.name %>
     <%= comment.content %>
     <%= link_to "댓글삭제", comment_path(comment), method: :delete %>
     </h4>
   <% end %>

<<<<<<< HEAD
   <%= render partial: 'comments/new', locals: { :comment => @event.comments.new , event: @event.id} %>
=======
   <%= render partial: 'comments/new', locals: { :comment => @event.comments.new , event: @event.id, belong: "event"} %>
>>>>>>> 5c1c87f677ba0d9e16c8eb8973eded3c90bc1672
