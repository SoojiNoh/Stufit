<!DOCTYPE html>
<html>
  <head>
    <style>
      .first-container {
        margin: 50px 0px 50px 0px;
        padding: 20px 40px 20px 40px;
      }
      .small-container {
        margin: 10px 0px 10px 0px;
        padding: 5px 0px 5px 0px;
      }
      .button-container {
        margin: 20px 0px 20px 0px;
        padding: 10px 0px 10px 0px;
      }
      body {
          background-color:  #64b5f6;
      }
      .fonty {
        letter-spacing: -1.3px;
      }
    </style>
  </head>
  <body class="fonty">
    <div id="login-page" class="row">
      <div class="col s12 z-depth-4 card-panel">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
          <%= devise_error_messages! %>
          <div class="row">
            <div class="input-field col s12 center">
              <div style="margin-top: 30px;">
                <%= image_tag "#{asset_path "http://i.imgur.com/KZFqXNw.png"}" %>
              </div>
              <p class="center login-form-text">온라인에서 시작하는 입시관리 stu-fit</p>
            </div>
          </div>
    
          <div class="row margin">
            <div class="input-field col s12">
              <i class="mdi-social-person-outline prefix"></i>
                <%= f.label :image, '이미지'%><br><br>  
                <%= f.file_field :image %>
            </div>
          </div>
      
          <div class="row margin">
            <div class="input-field col s12">
              <i class="mdi-social-person-outline prefix"></i>
                <%= f.label :membertype, '구분', class: "center-align" %><br>
                <%= f.radio_button :membertype, "highschool", class: "center-align", :checked => true %>
                <%= f.label :membertype, '고등학생', :value => "highschool", class: "center-align" %>
                <%= f.radio_button :membertype, "univ", class: "center-align" %>
                <%= f.label :membertype, '대학생', :value=> "univ", class: "center-align" %>
            </div>
          </div>
          <div class="row margin">
            <div class="input-field col s12">
              <i class="small material-icons prefix">person_pin</i>
                <%= f.label :name, '이름', class: "center-align" %><br>
                <%= f.text_field :name, id:"username" %>
            </div>
          </div>
    
    
          <div class="row margin">
            <div class="input-field col s12">
              <i class="mdi-communication-email prefix"></i>
                <%= f.label :email, '이메일' %><br>
                <%= f.email_field :email, autofocus: true %>
            </div>
          </div>
    
          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
          <% end %>
    
          <div class="row margin">
            <div class="input-field col s12">
              <i class="mdi-action-lock-outline prefix"></i>
                <%= f.label :password, '비밀번호' %> <i><br><br>(바꾸고 싶지 않으면 빈 칸으로 남겨두세요)</i><br />
                <%= f.password_field :password, autocomplete: "off" %>
                <% if @minimum_password_length %>
                  <br />
                  <em>최소 <%= @minimum_password_length %>자리</em>
                <% end %>
            </div>
          </div>
          <div class="row margin">
            <div class="input-field col s12">
              <i class="mdi-action-lock-outline prefix"></i>
                <%= f.label :password_confirmation, '비밀번호 확인' %><br />
                <%= f.password_field :password_confirmation, autocomplete: "off" %>
            </div>
          </div>
    
          <div class="row margin">
            <div class="input-field col s12">
              <i class="small material-icons prefix">insert_chart</i>
                <%= f.label :highschool, "출신 고등학교" %><br>
                <%= f.text_field :highschool, id:"highschool" %>
            </div>
          </div>
    
          <div class="row margin">
            <div class="input-field col s12">
              <i class="mdi-action-lock-outline prefix"></i>
                <%= f.label :current_password, '현재 비밀번호' %> <i><br><br>(변경을 위해 현재 비밀번호를 입력해주세요)</i><br />
                <%= f.password_field :current_password, autocomplete: "off" %>
            </div>
          </div>
          <div class="row margin">
            <div class="input-field col s12 button-container">
              <%= f.submit "비밀번호 변경", class: "btn waves-effect waves-light col s12" %>
            </div>
          </div>
        <% end %> <!--form_for-->
        
        <div class="small-container" align="right">
          <p>별로예요?ㅠㅠ <%= button_to "계정 삭제" , registration_path(resource_name), data: { confirm: "확실합니까??" }, method: :delete, class: "btn waves-effect" %></p>
          <%= link_to "뒤로", :back %>
        </div>

      </div>
    </div>
    
    <!--<script charset="utf-8">
      //$(function(){
        //setTimeout(function() {
          //$('body').addClass('cyan');      
        //}, 50);
      //});
    //</script>-->
    
  </body>
</html>